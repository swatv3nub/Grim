# Scheduled Scans

- Use `scan_schedule.php` as a template for scheduled scans.
- Add to your system's cron (Linux/macOS) or Task Scheduler (Windows).
- Edit the script to set your scan targets and options.
