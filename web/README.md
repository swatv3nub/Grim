# GRIM Web Dashboard (Stub)

- Access via `php -S localhost:8080 -t web` and open http://localhost:8080
- Shows scan history from the SQLite database.
- Extend to add scan controls, result viewing, and scheduling.
